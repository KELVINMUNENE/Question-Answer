{{#if addNewQuestion}}
<h1>New Question</h1>
<div>
 <form>
   <div class="form-group">
     <label for="question2017">Ask Question</label>
     {{input value=text id="query"}}
   </div>
   <button {{action 'saveQuestion'}}>Save</button>
 </form>
</div>
{{else}}
  <button {{action 'questionFormShow'}}>New question</button>
{{/if}}
